export class Items {
    todoList: string;
    dueDate: string;
    
    
    
      constructor(string?: string, date?:string) {
         this.todoList = string;
         this.dueDate = date;
      
      }
  }